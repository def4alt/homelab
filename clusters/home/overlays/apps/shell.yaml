apiVersion: kustomize.toolkit.fluxcd.io/v1
kind: Kustomization
metadata:
  name: shell
  namespace: flux-system
spec:
  interval: 10m
  path: ./apps/shell
  dependsOn:
    - name: namespaces
    - name: longhorn
  prune: true
  sourceRef:
    kind: GitRepository
    name: flux-system
  timeout: 10m
  wait: true
  decryption:
    provider: sops
    secretRef:
      name: sops-age
