apiVersion: kustomize.toolkit.fluxcd.io/v1
kind: Kustomization
metadata:
  name: minecraft
  namespace: flux-system
spec:
  interval: 10m
  path: ./apps/minecraft
  prune: true
  sourceRef:
    kind: GitRepository
    name: flux-system
  dependsOn:
    - name: namespaces
    - name: local-storage
    - name: metallb-config
  timeout: 10m
  wait: true
  decryption:
    provider: sops
    secretRef:
      name: sops-age
