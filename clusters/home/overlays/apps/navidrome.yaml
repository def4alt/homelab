apiVersion: kustomize.toolkit.fluxcd.io/v1
kind: Kustomization
metadata:
  name: navidrome
  namespace: flux-system
spec:
  interval: 10m
  path: ./apps/navidrome
  prune: true
  sourceRef:
    kind: GitRepository
    name: flux-system
  dependsOn:
    - name: namespaces
    - name: local-storage
    - name: cert-manager-issuers
    - name: traefik
  timeout: 10m
  wait: true
  decryption:
    provider: sops
    secretRef:
      name: sops-age
