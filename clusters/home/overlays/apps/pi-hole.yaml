apiVersion: kustomize.toolkit.fluxcd.io/v1
kind: Kustomization
metadata:
  name: pi-hole
  namespace: flux-system
spec:
  interval: 10m
  path: ./apps/pi-hole
  prune: true
  sourceRef:
    kind: GitRepository
    name: flux-system
  dependsOn:
    - name: longhorn
    - name: traefik
  timeout: 10m
  wait: true
  decryption:
    provider: sops
    secretRef:
      name: sops-age
