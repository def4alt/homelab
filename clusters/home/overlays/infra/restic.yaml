apiVersion: kustomize.toolkit.fluxcd.io/v1
kind: Kustomization
metadata:
  name: restic
  namespace: flux-system
spec:
  interval: 10m
  path: ./apps/restic
  prune: true
  sourceRef:
    kind: GitRepository
    name: flux-system
  dependsOn:
    - name: longhorn
  timeout: 10m
  wait: true
  decryption:
    provider: sops
    secretRef:
      name: sops-age
