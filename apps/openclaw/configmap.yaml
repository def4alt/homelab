apiVersion: v1
kind: ConfigMap
metadata:
  name: openclaw-config
  namespace: ai
data:
  openclaw.json: |
    {
      "agent": {
        "model": "venice/minimax-m25"
      },
      "memorySearch": {
        "extraPaths": ["notes", "ideas"]
      },
      "channels": {
        "telegram": {
          "enabled": true,
          "dmPolicy": "pairing"
        }
      }
    }
