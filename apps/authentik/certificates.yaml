apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: authentik-tls
  namespace: authentik
spec:
  secretName: authentik-tls
  commonName: auth.def4alt.com
  dnsNames:
    - auth.def4alt.com
  issuerRef:
    name: letsencrypt-prod
    kind: ClusterIssuer
