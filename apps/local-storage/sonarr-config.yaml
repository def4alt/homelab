apiVersion: v1
kind: PersistentVolume
metadata:
  name: sonarr-config
spec:
  capacity:
    storage: 5Gi
  accessModes:
    - ReadWriteOnce
  persistentVolumeReclaimPolicy: Retain
  storageClassName: manual-local
  claimRef:
    namespace: transmission
    name: sonarr-config
  hostPath:
    path: /var/lib/k8s-local-storage/transmission/sonarr-config
    type: DirectoryOrCreate
