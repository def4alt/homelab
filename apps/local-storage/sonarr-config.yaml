apiVersion: v1
kind: PersistentVolume
metadata:
  name: perun-sonarr-config-local
spec:
  capacity:
    storage: 5Gi
  accessModes:
    - ReadWriteOnce
  persistentVolumeReclaimPolicy: Retain
  storageClassName: manual-local
  claimRef:
    namespace: transmission
    name: sonarr-config-local
  hostPath:
    path: /var/lib/k8s-local-storage/transmission/sonarr-config
    type: DirectoryOrCreate
