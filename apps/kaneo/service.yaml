apiVersion: v1
kind: Service
metadata:
  name: kaneo
  namespace: kaneo
spec:
  selector:
    app: kaneo
  ports:
    - name: web
      port: 80
      targetPort: web
    - name: api
      port: 1337
      targetPort: api
