apiVersion: postgresql.cnpg.io/v1
kind: ScheduledBackup
metadata:
  name: kaneo-db-weekly
  namespace: kaneo
spec:
  schedule: "0 15 2 * * 6"
  backupOwnerReference: self
  cluster:
    name: kaneo-db
  method: plugin
  pluginConfiguration:
    name: barman-cloud.cloudnative-pg.io
