apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: shell
resources:
  - configmap.yaml
  - pvc.yaml
  - statefulset.yaml
  - service.yaml
  - secrets/ssh-authorized-keys.sops.yaml
decryption:
  provider: sops
  secretRef:
    name: sops-age
