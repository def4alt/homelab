apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - ../../../kaneo
patches:
  - target:
      group: apps
      version: v1
      kind: Deployment
      name: kaneo
      namespace: kaneo
    patch: |-
      - op: replace
        path: /spec/replicas
        value: 0
