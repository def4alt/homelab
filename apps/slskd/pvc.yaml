apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: slskd-config
  namespace: transmission
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: manual-local
  volumeName: slskd-config
  resources:
    requests:
      storage: 5Gi
