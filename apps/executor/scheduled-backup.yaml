apiVersion: postgresql.cnpg.io/v1
kind: ScheduledBackup
metadata:
  name: executor-db-weekly
  namespace: executor
spec:
  schedule: "0 15 3 * * 3"
  backupOwnerReference: self
  cluster:
    name: executor-db
  method: plugin
  pluginConfiguration:
    name: barman-cloud.cloudnative-pg.io
