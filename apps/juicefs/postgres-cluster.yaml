apiVersion: postgresql.cnpg.io/v1
kind: Cluster
metadata:
  name: juicefs-db
  namespace: juicefs
spec:
  instances: 1
  bootstrap:
    initdb:
      database: juicefs
      owner: juicefs
      secret:
        name: juicefs-auth
  storage:
    size: 10Gi
  monitoring:
    enablePodMonitor: true
