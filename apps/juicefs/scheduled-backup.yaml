apiVersion: postgresql.cnpg.io/v1
kind: ScheduledBackup
metadata:
  name: juicefs-db-weekly
  namespace: juicefs
spec:
  schedule: "0 15 20 * * 4"
  backupOwnerReference: self
  cluster:
    name: juicefs-db-local
  method: plugin
  pluginConfiguration:
    name: barman-cloud.cloudnative-pg.io
