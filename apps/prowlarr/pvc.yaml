apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: prowlarr-config-local
  namespace: transmission
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: manual-local
  volumeName: perun-prowlarr-config-local
  resources:
    requests:
      storage: 5Gi
