apiVersion: postgresql.cnpg.io/v1
kind: ScheduledBackup
metadata:
  name: linkwarden-db-daily
  namespace: linkwarden
spec:
  schedule: "0 30 3 * * *"
  backupOwnerReference: self
  cluster:
    name: linkwarden-db
  method: plugin
  pluginConfiguration:
    name: barman-cloud.cloudnative-pg.io
