apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: sonarr-config-local
  namespace: transmission
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: manual-local
  volumeName: perun-sonarr-config-local
  resources:
    requests:
      storage: 5Gi
