apiVersion: postgresql.cnpg.io/v1
kind: Database
metadata:
  name: immich
  namespace: immich
spec:
  name: immich
  owner: immich
  cluster:
    name: immich-db-local
  extensions:
    - name: vector
    - name: cube
    - name: earthdistance
