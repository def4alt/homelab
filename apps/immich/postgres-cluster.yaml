apiVersion: postgresql.cnpg.io/v1
kind: Cluster
metadata:
  name: immich-db
  namespace: immich
spec:
  instances: 1
  bootstrap:
    initdb:
      database: immich
      owner: immich
      secret:
        name: immich-db-auth
  storage:
    size: 5Gi
