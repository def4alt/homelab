apiVersion: postgresql.cnpg.io/v1
kind: Cluster
metadata:
  name: immich-db
  namespace: immich
spec:
  instances: 1
  bootstrap:
    recovery:
      source: pre-rename
  storage:
    size: 20Gi
    storageClass: manual-local
  monitoring:
    enablePodMonitor: true
  plugins:
    - name: barman-cloud.cloudnative-pg.io
      isWALArchiver: true
      parameters:
        barmanObjectName: cnpg-barman-s3
        serverName: immich-db-20260718
  externalClusters:
    - name: pre-rename
      plugin:
        name: barman-cloud.cloudnative-pg.io
        parameters:
          barmanObjectName: cnpg-barman-s3
          serverName: immich-db-local
